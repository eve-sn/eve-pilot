DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

POSTGRES_DB=eve_pilot
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432

# --- Email ---
# Laisser EMAIL_BACKEND vide (ou commente) en dev : les mails s'affichent
# dans la console. Pour l'envoi reel via le SMTP d'EVE, decommenter et
# renseigner les identifiants (ne JAMAIS commiter le mot de passe reel).
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=vps75468.serveur-vps.net
EMAIL_PORT=465
EMAIL_USE_SSL=True
EMAIL_USE_TLS=False
# EMAIL_HOST_USER=alydiouf@eve-sn.org
# EMAIL_HOST_PASSWORD=
EMAIL_TIMEOUT=10
DEFAULT_FROM_EMAIL=EVE Pilot <no-reply@eve-sn.org>
SITE_BASE_URL=http://127.0.0.1:8000
